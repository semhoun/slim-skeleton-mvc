{% extends 'layout.twig' %}

{% block contents %}
    <h1>Blog</h1>

    <div class="bg-light p-5 rounded">
        {% if post %}
            <h2>{{ post.title }}</h2>
            <hr>
            <p>{{ post.content }}</p>
        {% endif %}
    </div>
{% endblock %}
